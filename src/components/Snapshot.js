import React from 'react';
import { Card, CardContent, Typography } from '@material-ui/core/'

const Snapshot = () => {
    
    return (
      <Card>
        <CardContent>
          <Typography>This will be a market snapshot</Typography>
        </CardContent>
      </Card>
    );
}